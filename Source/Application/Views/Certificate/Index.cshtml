@model CertificateViewModel
<div class="card">
	<div class="card-header">
		<h1 class="h4 m-0 p-0">Certificate</h1>
	</div>
	<div class="card-body">
		<p>Create a certificate.</p>
		<partial name="ValidationSummary" />
		<partial for="Form" name="Form" />
	</div>
</div>